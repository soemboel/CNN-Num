torch==2.14.0
torchvision==0.29.0
onnx>=1.16.0
numpy
pillow

# only needed for the optional Flask server path (app.py) - not required
# to train the model or export it to ONNX for the browser-based frontend
flask==3.0.3
flask-cors==4.0.1
opencv-python-headless==4.10.0.84
gunicorn==22.0.0
